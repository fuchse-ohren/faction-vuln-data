Webサーバーで使用可能なHTTPメソッドは複数あります（`OPTIONS`、`HEAD`、`GET`、`POST`、`PUT`、`DELETE`など）。それぞれのメソッドは異なる機能を持ち、その使用を許可する場合に異なるレベルのセキュリティリスクが伴います。

クライアントはリクエスト内で`OPTIONS`メソッドを使用することで、サーバーがどのメソッドを許可しているかを確認することができます。

攻撃者はほぼ確実にこの簡単なテストを実施します。これにより、サーバーで許可されている高リスクなメソッドの有無を迅速に確認できるためです。
