HTTP 自体はステートレス プロトコルです。したがって、サーバーは機能しません
どのリクエストがどのクライアントによって実行されるかを判断するため
クライアントは認証されているか、または認証されていません。

HTTP Cookie の使用
ヘッダー内で、Web サーバーが各個人を識別できるようにします。
したがって、どのクライアントが有効なデータを保持しているかを判断できます。
認証しないものから。これらはセッションとして知られています
クッキー。

サーバーによって Cookie が設定されると (ヘッダーに送信されます)、
HTTP 応答) を設定するために設定できるいくつかのフラグがあります
Cookie のプロパティと、Cookie がどのように処理されるか
ブラウザ。

これらのフラグの 1 つは、「セキュア」フラグとして知られています。とき
セキュアフラグが設定されている場合、ブラウザはそれがネットワーク経由で送信されるのを防ぎます。
クリア テキスト チャネル (HTTP) を使用し、次の場合にのみ送信を許可します。
暗号化されたチャネルが使用されます (HTTPS)。

ツールは Cookie を検出しました。
セキュアフラグが設定されていない状態でサーバーによって設定されました。とはいえ、
この Cookie の初期設定は HTTPS 接続、任意の HTTP 経由で行われました。
同じサーバーにリンクすると、Cookie がクリアで送信されます。
文章。