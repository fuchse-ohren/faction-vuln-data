Webサーバーで使用できるHTTPメソッドは多数あります。
(例: `OPTIONS`、`HEAD`、`GET`、`POST`、`PUT`、`DELETE`など)。
これらのメソッドはそれぞれ異なる機能を実行し、それぞれに
Web サーバーで使用が許可されている場合の、関連するリスクのレベル。
Apache の `.htaccess` ファイル内の `<Limit>` ディレクティブにより、次のことが可能になります。
管理者は、どの方法を使用するかを定義します。
ブロック。ただし、これはブラックリストに登録するアプローチであるため、避けられません。
サーバー管理者が特定の HTTP の追加を誤って見逃してしまう可能性があるため
方法がブロックされるため、リスクレベルが高まります。
アプリケーションやサーバー。