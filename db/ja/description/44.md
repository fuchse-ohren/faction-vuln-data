最新の Web アプリケーションは、いくつかの異なるものに依存します。
プログラミング言語。

これらの言語は 2 つに分類できます
味。これらはクライアント側の言語 (次の言語で実行される言語など) です。
ブラウザ -- JavaScript など) とサーバー側言語 (
サーバー (ASP、PHP、JSP など) によって実行され、
動的ページ (クライアント側のコード) がクライアントに送信されます。
すべてのサーバー側コードはサーバーによって実行される必要があるため、
クライアントには決して見られるべきではありません。ただし、一部のシナリオでは、
可能性のあること:


1. サーバー側のコードに構文エラーがあるため、実行されません。
サーバーによって送信されますが、代わりにクライアントに送信されます

2. 細工されたリクエストを使用すると、サーバーに強制的にアクセスさせることが可能です
アプリケーションを実行せずにソースコードを表示します。



サーバー側のソースコードには機密情報が含まれることが多いため、
データベース接続文字列や詳細などの情報
アプリケーションのワークフローでは、これは非常に危険な可能性があります。

攻撃者
偶然または強制的にページを検出しようとします。
サーバー側のソースコードの公開を許可し、
さらなる脆弱性や機密情報の発見。

ツール
サーバーの応答内でサーバー側のソース コードが検出されました。
_(次のようなバイナリ ファイルをリクエストすると、誤検知が発生する可能性があります。
画像 (.JPG または .PNG) であり、手動による検証が必要な場合があります。)_