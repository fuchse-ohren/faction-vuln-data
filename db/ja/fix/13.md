信頼できないデータをファイルの形成に使用しないことをお勧めします。
含める場所。

データを検証するには、アプリケーションは、提供された 
ファイルの値は許可されます。これは、次のことを実行することで実現できます。 
パラメータ値をホワイトリストに登録し、それをリストと照合する
許可されたファイルの数。指定された値がどの値とも一致しない場合
ホワイトリストに含まれている場合、サーバーは標準にリダイレクトする必要があります。
エラーページ。

動的コンテンツが要求されている一部のシナリオでは、
信頼できるリソースのリストに対して検証を実行することはできません。
したがって、リストも動的になる必要があります (ファイルの変更に応じて更新されます)。
または、フィルタリングを実行して無関係なユーザー入力 (セミコロン、
ピリオドなど)、「a-z0-9」のみを許可します。

また、機密ファイルは次の場所に保存しないことをお勧めします。
webroot とユーザー権限がディレクトリによって強制されること
は正しいです。