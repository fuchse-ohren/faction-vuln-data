使用されているフレームワークに応じて、実装方法は次のようになります。
ただし、「Strict-Transport-Security」を使用することをお勧めします。
ヘッダーはサーバー上で構成されます。

このためのオプションの 1 つ
ヘッダーは「max-age」で、これはミリ秒単位での表現です。
クライアントのブラウザが遵守する時間を決定する
ヘッダーポリシー。

環境やアプリケーションによっては、
期間は、数分程度の場合もあれば、数日程度の場合もあります。