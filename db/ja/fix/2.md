信頼できないデータを LDAP の形成に使用しないことをお勧めします。
クエリ。

データを検証するには、アプリケーションは次のことを確認する必要があります。
指定された値には、必要な文字のみが含まれています。
必要なアクションを実行します。たとえば、ユーザー名は次のようになります。
必須の場合、英数字以外の文字は受け入れられません。

これが不可能な場合は、特殊文字をエスケープする必要があります。
それに応じて扱われます。次の文字はエスケープする必要があります。
バックスラッシュ:

*「&」
*「!」
* `|`
* `=`
*「<」
* `>`
*「、」
* `+`
* `-`
*「」
*「」
* `;`

追加の文字フィルタリングを以下に適用する必要があります。

*`(` 
* `)`
* `\`
* `/`
* `*`
* `NULL`

これらの文字には ASCII エスケープが必要です。