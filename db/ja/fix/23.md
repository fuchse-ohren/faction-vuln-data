可能な場合は、HTTP `PUT` メソッドをグローバルに無効にする必要があります。これ
通常、簡単な設定変更で実行できます。
サーバー。 「PUT」メソッドを無効にする手順は、環境によって異なります。
使用されているサーバーの種類 (IIS、Apache など)。

アプリケーションに応じて `PUT` メソッドが必要な場合
REST スタイルの Web サービスなどの機能、厳しい制限
安全性 (SSL/TLS が有効) のみを保証するために実装する必要があります。
許可されたクライアントは `PUT` メソッドの使用を許可されます。

さらに、サーバーのファイル システムのアクセス許可も必要です
厳格な制限を強制します。