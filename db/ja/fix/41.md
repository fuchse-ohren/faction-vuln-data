Web サーバーが静的ファイルとファイルを共有するために利用されている場合を除きます。
機密性のないファイルの場合、ディレクトリの一覧表示を有効にすることは不適切であると考えられます。
セキュリティの実践

これは通常、単純な方法で実行できます
サーバー上の設定変更。ディレクトリを無効にする手順
リストは、使用されているサーバーの種類 (IIS、
アパッチなど）。ディレクトリのリストが必要で許可されている場合は、
このような構成のリスクを確実にするための措置を講じる必要があります。
削減されます。

これらには次のものが含まれます。

1. 影響を受けるページにアクセスするには認証が必要です。

2. 影響を受けるパスを「robots.txt」ファイルに追加して、
ディレクトリの内容は検索エンジン経由で検索可能です。

3. 機密ファイルが保存されていないことを確認します。
Web またはドキュメントのルート。

4. アプリケーションに必要のないファイルを削除します。
機能。