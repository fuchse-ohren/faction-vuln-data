信頼できないデータをコマンドの作成に使用しないことをお勧めします。
OSによって実行されます。

データを検証するには、アプリケーションは指定された値が 
を実行するために必要な文字のみが含まれています。
必要なアクション。

たとえば、フォームフィールドに IP アドレスが必要な場合、数値のみが必要です。
ピリオドも受け入れられる必要があります。さらに、すべての制御演算子
(`&`、`&&`、`|`、`||`、`$`、`\`、`#`) は明示的に拒否する必要があります
そしてサーバーによって有効な入力として受け入れられることはありません。